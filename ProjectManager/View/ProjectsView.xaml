<UserControl x:Class="ProjectManager.View.ProjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjectManager.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:vm="clr-namespace:ProjectManager.ViewModel"
             d:DataContext="{d:DesignInstance Type=vm:ProjectsViewViewModel}">
    <UserControl.DataContext>
        <vm:ProjectsViewViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <ui:CommandBar Grid.Row="0" Grid.Column="0">
            <ui:AppBarButton Icon="Add" Command="{Binding CreateProjectCommand}"/>
            <ui:AppBarButton Icon="Edit" Command="{Binding EditProjectCommand}"/>
            <ui:AppBarButton Icon="Delete" Command="{Binding DeleteProjectCommand}"/>
            <ui:AppBarButton Icon="Filter" Command="{Binding FilterProjectsCommand}"/>
        </ui:CommandBar>
        <ui:ListView Grid.Row="2" ItemsSource="{Binding Projects}"
                          d:ItemsSource="{d:SampleData ItemCount=2}"
                     SelectedItem="{Binding SelectedProject}"
                     Name="l_projects">
            <ui:ListView.ItemTemplate>
                <DataTemplate>
                    <ui:SimpleStackPanel Margin="0 7" Spacing="2">
                        <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Responsibility.Fullname}" FontSize="13"/>
                    </ui:SimpleStackPanel>
                </DataTemplate>
            </ui:ListView.ItemTemplate>
        </ui:ListView>
        <Grid Grid.Column="1" Grid.Row="0" Grid.RowSpan="3" Grid.ColumnSpan="2" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="3*" />
            </Grid.RowDefinitions>
            
            <TabControl Grid.Row="1" TabStripPlacement="Left" >
                <TabItem Header="Aufgaben">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="1*" />
                        </Grid.RowDefinitions>
                        <ui:CommandBar>
                            <ui:AppBarButton Icon="Add"/>
                            <ui:AppBarButton Icon="Edit"/>
                            <ui:AppBarButton Icon="Delete"/>
                            <ui:AppBarButton Icon="Sort"/>
                        </ui:CommandBar>
                        <ui:ListView Grid.Row="1">
                            
                        </ui:ListView>
                    </Grid>
                </TabItem>
                <TabItem Header="Kommentare">
                    
                </TabItem>
                <TabItem Header="Änderungsprotokoll">
                    
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</UserControl>
